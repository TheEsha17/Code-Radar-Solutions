#include <stdio.h>

int main() {
    int a,b;
    scanf{"%d %d",&a,&b};
    result=pow(b,2);
    if(a==result){
        printf("Yes");
    }
    else{
        printf("No");
    }
    return 0;
}
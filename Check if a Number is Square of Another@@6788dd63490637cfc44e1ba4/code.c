#include <stdio.h>
#include<math.h>
int main() {
    int a,b,result;
    scanf{"%d %d",&a,&b};
    result=pow(b,2);
    if(a==result){
        printf("Yes");
    }
    else{
        printf("No");
    }
    return 0;
}